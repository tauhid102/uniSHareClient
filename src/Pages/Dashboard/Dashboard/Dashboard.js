import React, { useContext } from 'react';
import Profile from '../Profile/Profile';

const Dashboard = () => {
    return (
        <div>
            <Profile></Profile>
        </div>
    );
};

export default Dashboard;